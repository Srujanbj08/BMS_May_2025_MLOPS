# Core ML & Data Libraries
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
xgboost>=1.5.0

# Web App
streamlit>=1.18.0

# Explainability
shap>=0.41.0
lime>=0.2.0.1

# Environment Management
python-dotenv>=0.21.0

# Logging (standard, no package needed unless advanced logging setup)
# No external logger needed, using built-in logging

# For compatibility (e.g., with older Pip versions)
joblib>=1.1.0

# Optional: version pinning for reproducibility
# matplotlib and scipy are often required as dependencies by SHAP and LIME
matplotlib>=3.4.0
scipy>=1.7.0
